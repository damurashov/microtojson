image: openbsd/latest
sources:
- https://git.sr.ht/~rkta/microtojson
tasks:
- check: |
    cd microtojson
    make ASAN= \
    CFLAGS='-std=c99 -O2 \
    -Wall -Wextra -pedantic -Wconversion -Werror -Wshadow \
    -fno-common \
    -Wno-missing-field-initializers' \
    -s
